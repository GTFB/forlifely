import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MemberJournalCreatePage() {
  return (
    <RolePageShell
      role="member"
      title="Create Journal Entry"
      description="Create a new journal record with notes and optional session linkage."
    />
  );
}

