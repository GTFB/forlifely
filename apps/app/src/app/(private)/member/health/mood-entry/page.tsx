import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MemberMoodEntryPage() {
  return (
    <RolePageShell
      role="member"
      title="Mood Entry"
      description="Mood capture with lightweight prompts and optional notes."
    />
  );
}

