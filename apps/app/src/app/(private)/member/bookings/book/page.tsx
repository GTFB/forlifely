import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MemberBookSessionPage() {
  return (
    <RolePageShell
      role="member"
      title="Book Session"
      description="Booking form with mentor, slot, session preferences, and credit validation."
    />
  );
}

