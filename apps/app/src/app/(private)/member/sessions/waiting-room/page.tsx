import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MemberSessionWaitingRoomPage() {
  return (
    <RolePageShell
      role="member"
      title="Session Waiting Room"
      description="Waiting room with readiness state and preview before the session starts."
    />
  );
}

