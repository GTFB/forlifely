import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MentorSessionWaitingRoomPage() {
  return (
    <RolePageShell
      role="mentor"
      title="Session Waiting Room"
      description="Participant wait state, audio/video preview, and session readiness status."
    />
  );
}

