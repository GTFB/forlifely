import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MentorSessionEndPage() {
  return (
    <RolePageShell
      role="mentor"
      title="End Session"
      description="Single end-session workflow with planned, early, and emergency termination scenarios."
    />
  );
}

