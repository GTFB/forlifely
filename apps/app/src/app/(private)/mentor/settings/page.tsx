import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MentorSettingsPage() {
  return (
    <RolePageShell
      role="mentor"
      title="Settings"
      description="Account info, security, payments, preferences, and inline availability editor."
    />
  );
}

