import { RolePageShell } from "@/components/cabinet/RolePageShell";

export default function MentorProfileViewPage() {
  return (
    <RolePageShell
      role="mentor"
      title="Profile View"
      description="Read-only mentor profile with rating, pitch, skills, and availability summary."
    />
  );
}

